const html=document.documentElement,toggle=document.querySelector(".nav-toggle"),nav=document.querySelector(".main-nav"),theme=document.querySelector(".theme-toggle"),header=document.querySelector(".site-header"),progress=document.querySelector(".page-progress"),topBtn=document.querySelector(".back-top");
const saved=localStorage.getItem("lh-theme");if(saved)html.dataset.theme=saved;
theme?.addEventListener("click",()=>{html.dataset.theme=html.dataset.theme==="dark"?"light":"dark";localStorage.setItem("lh-theme",html.dataset.theme)});
toggle?.addEventListener("click",()=>{const o=nav.classList.toggle("open");toggle.setAttribute("aria-expanded",String(o))});
document.querySelectorAll(".main-nav a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")));
window.addEventListener("scroll",()=>{const y=scrollY,max=document.documentElement.scrollHeight-innerHeight;header?.classList.toggle("scrolled",y>10);topBtn?.classList.toggle("show",y>500);if(progress&&max>0)progress.style.width=(y/max*100)+"%"});
topBtn?.addEventListener("click",()=>scrollTo({top:0,behavior:"smooth"}));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");io.unobserve(e.target)}}),{threshold:.13});document.querySelectorAll(".reveal").forEach(e=>io.observe(e));
const co=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,t=+el.dataset.count,s=performance.now();function run(n){const p=Math.min((n-s)/1100,1);el.textContent=Math.floor(t*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(run);else el.textContent=t}requestAnimationFrame(run);co.unobserve(el)}),{threshold:.5});document.querySelectorAll("[data-count]").forEach(e=>co.observe(e));